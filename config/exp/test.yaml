# @package _global_
tag: profiler

model:
  task:
    task: ModelTask.infill
  readin_strategy: EmbedStrat.token
dataset:
  bin_size_ms: 5
  datasets:
  - mc_maze_small
  # - mc_maze
  data_keys:
  - "spikes"

  max_channels: 137 # test # TODO
train:
  epochs: 2
  batch_size: 128
  profiler: "simple"
