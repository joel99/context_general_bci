# @package _global_
tag: test

defaults:
  - /model: pretrain
  - /train: small
model:
  readin_strategy: EmbedStrat.token
dataset:
  bin_size_ms: 5
  datasets:
  # - mc_maze_small
  - churchland_maze_jenkins-.*
  - churchland_maze_nitschke-.*

  data_keys:
  - "spikes"

  max_arrays: 2
  max_channels: 96
train:
  epochs: 100
  # epochs: 2000
  batch_size: 128